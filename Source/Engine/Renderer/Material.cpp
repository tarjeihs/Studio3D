#include "EnginePCH.h"
#include "Material.h"

void CMaterial::Render(const CCamera& Camera)
{
    //Shader->Use();
    //Shader->SetMat4("model", Camera.GetViewModel(Actor->Transform));
    //Shader->SetMat4("view", Camera.GetViewMatrix());
    //Shader->SetMat4("projection", Camera.GetProjectionMatrix());
    //Shader->Render();
}
